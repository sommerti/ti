<h1>For Admin: View All Trips</h1>

<!-- show all public trips -->
<% if @public_trips.blank? %>
	<p>No public trip.</p>
<% else %>
	<h2>Public Trips:</h2>
	<%= render @public_trips %>
<% end %>

<hr>

<!-- show all private trips -->
<% if @private_trips.blank? %>
	<p>No private trip.</p>
<% else %>
	<h2>Private Trips:</h2>
	<%= render @private_trips %>
<% end %>

<p><%= link_to 'New Trip', new_trip_path %></p>
